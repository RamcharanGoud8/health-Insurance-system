server:
  port: 8080

spring:
  application:
    name: APPLICATION-REGISTRATION-SERVICE

  datasource:
    url: jdbc:mysql://mysql:3306/his_project
    username: root
    password: Ramcharan8@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    "org.hibernate.SQL": DEBUG
eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka