server:
  port: 8086

spring:
  application:
    name: AUTH-SERVICE

  datasource:
    url: jdbc:mysql://mysql:3306/his_project
    username: root
    password: Ramcharan8@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: HISProjectSecretKeyForJWTTokenGenerationAndValidation2024
  expiration: 86400000

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka